type: firebase-socket-datasource
# Add any config options your datasource expects here
config:
databaseURL: ${FIREBASE_DATABASE_URL} # for render.com in node env
socketURL: ${SOCKET_URL}  # URL of your Socket.IO server 